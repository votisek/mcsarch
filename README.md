# This branch acts as a repository for MCSaRch

This is used for the vital packages that are kept here at the most stable and newest version

To install this repo on yur arch system, run the following command:

```bash
curl repo.vojta.fyi/install.sh | sh
```
